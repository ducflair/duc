
// Implementation for measurement system Surface -> Leveraged by Library

// SVG Theme processor to change the styles on the SVG's -> Leveraged by Library


// Here We would have the file on a pure text data using JSON
- We could have a preview if the package for the duc file is installed
- This could be opened by our app
    - We would make a reader of the structured data to rely on the app and then saved onto the .duc file