"""Development-only helpers for ducpy.

This package is intentionally excluded from the published wheel (see
``pyproject.toml`` ``[tool.maturin].python-packages``). It hosts utilities
used by the local test suite, examples, and CI scripts that should never
be shipped to end users.
"""
